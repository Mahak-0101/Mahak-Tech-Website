import { products } from "@/lib/data";import { ButtonLink, Card, Section } from "@/components/ui";export function generateStaticParams(){return products.map(p=>({category:p.toLowerCase().replaceAll(" ","-")}))}export default async function Product({params}:{params:Promise<{category:string}>}){const {category}=await params;const name=category.split("-").map(w=>w[0].toUpperCase()+w.slice(1)).join(" ");return <Section eyebrow="Coming Soon" title={`${name} marketplace by MahakTech.`}><Card><p className="text-xl text-slate-300">MahakTech is preparing a curated marketplace for {name.toLowerCase()} products with secure checkout, subscriptions, documentation, and enterprise onboarding.</p><div className="mt-8"><ButtonLink href="/contact">Notify Me</ButtonLink></div></Card></Section>}
