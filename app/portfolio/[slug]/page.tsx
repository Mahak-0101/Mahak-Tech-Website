import { notFound } from "next/navigation";import { portfolio } from "@/lib/data";import { Card, Section } from "@/components/ui";export function generateStaticParams(){return portfolio.map(({slug})=>({slug}))}export default async function Case({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=portfolio.find(x=>x.slug===slug);if(!p)notFound();return <Section eyebrow={p.sector} title={p.title}><div className="grid gap-6 md:grid-cols-2">{["Problem","Solution","Features","Outcome","Performance","Client Review"].map(h=><Card key={h}><h2 className="text-2xl font-bold">{h}</h2><p className="mt-3 text-slate-400">{h==="Outcome"?p.outcome:"MahakTech delivered a secure, scalable, polished experience with executive dashboards, automation, and resilient integrations."}</p></Card>)}</div><div className="mt-6 flex flex-wrap gap-2">{p.tech.map(t=><span className="rounded-full bg-white/10 px-3 py-1 text-sm" key={t}>{t}</span>)}</div></Section>}
