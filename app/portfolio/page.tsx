import { portfolio } from "@/lib/data";import { Card, Section } from "@/components/ui";import Link from "next/link";export const metadata={title:"Portfolio"};export default function Portfolio(){return <Section eyebrow="Case Studies" title="Professional case studies with measurable outcomes."><div className="grid gap-6 md:grid-cols-3">{portfolio.map(p=><Link href={`/portfolio/${p.slug}`} key={p.slug}><Card><p className="text-cyan-300">{p.sector}</p><h2 className="mt-3 text-2xl font-bold">{p.title}</h2><p className="mt-3 text-slate-400">{p.outcome}</p></Card></Link>)}</div></Section>}
