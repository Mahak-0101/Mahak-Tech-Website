import type { MetadataRoute } from "next";import { products, services, portfolio } from "@/lib/data";export default function sitemap():MetadataRoute.Sitemap{const base="https://mahaktech.com";return ["","/about","/services","/portfolio","/blog","/contact","/consultation","/dashboard/client","/dashboard/admin",...services.map(s=>`/services/${s.slug}`),...portfolio.map(p=>`/portfolio/${p.slug}`),...products.map(p=>`/products/${p.toLowerCase().replaceAll(" ","-")}`)].map(url=>({url:`${base}${url}`,lastModified:new Date()}))}
