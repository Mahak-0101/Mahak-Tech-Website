import { services } from "@/lib/data";import { Card, Section } from "@/components/ui";import Link from "next/link";export const metadata={title:"Services"};export default function Services(){return <Section eyebrow="Services" title="Premium engineering services for every stage of digital transformation."><div className="grid gap-5 md:grid-cols-3">{services.map(s=><Link href={`/services/${s.slug}`} key={s.slug}><Card><s.icon className="text-cyan-300"/><h2 className="mt-5 text-2xl font-bold">{s.title}</h2><p className="mt-3 text-slate-400">{s.blurb}</p></Card></Link>)}</div></Section>}
