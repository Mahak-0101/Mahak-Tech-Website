import { notFound } from "next/navigation";import { services } from "@/lib/data";import { ButtonLink, Card, Section } from "@/components/ui";export function generateStaticParams(){return services.map(({slug})=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);return {title:s?.title??"Service",description:s?.blurb}}export default async function Service({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();return <><Section eyebrow="MahakTech Service" title={s.title}><p className="max-w-3xl text-xl text-slate-300">{s.blurb} We combine strategy, interface design, secure architecture, analytics, and maintainable delivery practices to create platforms that scale.</p><div className="mt-10 grid gap-6 md:grid-cols-3">{["Benefits","Process","Technology"].map((h,i)=><Card key={h}><h2 className="text-2xl font-bold">{h}</h2><p className="mt-3 text-slate-400">{i===0?"Faster launches, better reliability, measurable ROI, and premium user experiences.":i===1?"Discovery, roadmap, design, sprint delivery, QA, launch, monitoring, and iteration.":s.tech.join(", ")}</p></Card>)}</div></Section><Section eyebrow="FAQ" title={`Common questions about ${s.title}.`}><div className="grid gap-4"><Card><h3 className="font-bold">How quickly can we start?</h3><p className="mt-2 text-slate-400">Most engagements begin with a discovery workshop within one business week.</p></Card><Card><h3 className="font-bold">Can MahakTech support after launch?</h3><p className="mt-2 text-slate-400">Yes. Maintenance plans include monitoring, security updates, backups, and roadmap improvements.</p></Card></div><div className="mt-8"><ButtonLink href="/consultation">Book {s.title} Consultation</ButtonLink></div></Section></>}
